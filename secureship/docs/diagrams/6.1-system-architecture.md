# 6.1 High-Level System Architecture

> Regenerated in Week 5 against the actual implementation.

```mermaid
flowchart TB
    subgraph Client["Browser / Frontend (React 18 + Vite)"]
        UI[Chat Window]
        Modal["6-digit Code Modal\n(rendered on code_sent state)"]
        AdminUI["Admin Panel\n(/admin/*)"]
    end

    subgraph Backend["Backend API (FastAPI + uvicorn)"]
        ChatAPI["POST /chat\nGET /chat/:id/state"]
        VerifyAPI["POST /verify-code"]
        AdminAPI["GET/POST/PUT/DELETE /admin/*"]
        ToolLayer["Tool Layer\ntools/identity.py + tools/shipments.py\nenforces identity gate BEFORE data access"]
        AuthDep["require_admin dependency\nauth/auth0.py — JWKS RS256"]
    end

    subgraph LLM["Local LLM Runtime (host machine)"]
        Ollama["Ollama :11434\nqwen3:8b — think: false"]
    end

    subgraph DataLayer["Data Layer (Postgres 16 :5433)"]
        DB[("customers\nshipments\npackages\nchat_sessions")]
    end

    subgraph IdP["Auth0 (admin only)"]
        Auth0["Auth0 tenant\nJWT issuer"]
    end

    UI -->|"POST /chat"| ChatAPI
    UI -->|"GET /chat/:id/state\npage-refresh restore"| ChatAPI
    ChatAPI -->|"messages + tool defs"| Ollama
    Ollama -->|"tool_call or reply"| ChatAPI
    ChatAPI --> ToolLayer
    ToolLayer -->|"SELECT WHERE customer_id = session.customer_id"| DB
    ChatAPI -->|"persist ChatSession transcript + state"| DB

    Modal -->|"POST /verify-code"| VerifyAPI
    VerifyAPI -->|"flip state → verified"| DB

    AdminUI -->|"Auth0 redirect"| Auth0
    Auth0 -->|"RS256 JWT"| AdminUI
    AdminUI -->|"Bearer token"| AdminAPI
    AdminAPI --> AuthDep
    AuthDep -.->|"JWKS fetch (cached)"| Auth0
    AuthDep -->|"validated"| DB

    style ToolLayer fill:#ffe6cc,stroke:#d79b00,stroke-width:2px
    style AuthDep fill:#ffe6cc,stroke:#d79b00,stroke-width:2px
```

**Key decisions reflected here:**

- **No separate session store.** All state lives in Postgres `chat_sessions`. `GET /chat/:id/state` restores full UI state on page refresh without an LLM call.
- **Identity gate is in the tool layer, not the prompt.** The model *requests* a tool; whether it executes depends on `session.state` and `session.customer_id` in the DB row.
- **Two identity systems never intersect.** `require_admin` validates Auth0 JWTs and never reads `ChatSession`. Chat routes never import from `auth/`. Proven by `scripts/test_separation.py`.
- **Ollama runs on the host**, not in Docker, to preserve Apple Metal GPU acceleration.
- **Shipment tool results are not replayed** from transcript — always re-queried so admin changes are immediately reflected in chat.
